<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quality</title>
</head>
<body style="text-align: center;">
    <div class="container">
    <div class="row">

        <% if(blogs.length<1){%>

        <p class="text-white">No Designs Post yet, create a new one</p>

        <%  } %>


        <% blogs.forEach(blog=>{ %>


        <div class="col-lg-6 card mt-2 mr-0">
            <div class="row p-2">

                <div class="col-5 overflow-hidden">
                    <img width="400" height="400" src="/uploads/images/<%= blog.img %>" class="card-img" alt="...">

                </div>

                <div class="col-7">
                    <div class="d-flex align-items-start">
                        <h5 class="w-100"><b> <%= blog.title %> </b></h5>
                    </div>

                    <span class="align-self-stretch "><%= blog.snippet %>

                    </span>
                    <div class="d-flex align-items-end">
                        <div class=" flex-grow-1">
                            <h5 class="" style="font-size: 14px; line-height: 0.5rem;">
                                <span><b><%= blog.author%></b></span></h5>
                            <p style="font-size: 8px; line-height: 0.2rem;">
                                <%= blog.timeCreated.toLocaleDateString() %></p>
                        </div>
                        <a href="/dno/<%=blog.slug %> " class=" align-self-end">
                            <i class='material-icons ml-3 text-black'>Copy Link</i>
                        </a>
                    </div>

                </div>
            </div>

        </div>


        <% }) %>




    </div>
</div>
</body>
</html>